# {{CLIENT_NAME}} - learnings

<!-- Rules, not events. Append when a lesson is confirmed, prune when invalidated.
     Test: does this change how the next task should be done? If not, it is log material. -->

- {{e.g. "Client approves copy only after seeing it in mockup form - always send visual context"}}
